# UView: a micro templating framework for PHP

UView is a minimalist plain PHP templating framework supporting template inheritance "à la Twig". For a usage exemple, look at the given index.php file and at the tempplates directory.
